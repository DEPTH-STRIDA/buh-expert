function update() {
    var mapheight = document.getElementById('contacts').offsetHeight;
    var mapwidth = document.getElementById('contacts').offsetWidth;
    document.getElementById('map').style.height=mapheight+"px"
    document.getElementById('map').style.width=mapwidth+"px"
  }
